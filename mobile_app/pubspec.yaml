name: plant_disease_detector
description: Offline Plant Disease Detection mobile app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  tflite_flutter: ^0.11.0
  image: ^4.2.0
  image_picker: ^1.1.0
  permission_handler: ^11.3.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/models/
